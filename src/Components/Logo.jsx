import React from 'react'
import styles from './jobcard.module.css'
import styled from 'styled-components'

export const Logo = ({ value }) => {
    return (
        <img src={value} alt="error in img" />
    )
}

